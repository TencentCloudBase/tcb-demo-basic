import { app, $app, $w } from '../../app/weapps-api';
export default {
  appJson: {
    // 小程序接口权限相关设置，可选
    /* permission: {
      'scope.userLocation': {
        desc: '你的位置信息将用于小程序位置接口的效果展示',
      },
    }, */
  },
}
  